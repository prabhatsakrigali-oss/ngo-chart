environments:
  dev: {}
  stage: {}
  prod: {}

---

releases:
  - name: frontend
    namespace: default
    chart: ..

    values:
      - ../values/common.yaml
      - ../values/{{ .Environment.Name }}.yaml
