environments:
  dev: {}
  stage: {}
  prod: {}

---

releases:
  - name: backend

    namespace: default

    chart: ..

    labels:
      app: backend

    values:
      - ../values/common.yaml
      - ../values/{{ .Environment.Name }}.yaml
